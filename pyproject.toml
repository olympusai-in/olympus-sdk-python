[project]
name = "olympus-sdk"
version = "0.1.0"
description = "Olympus AI — Log ingestion SDK for Python"
requires-python = ">=3.9"
license = {text = "MIT"}

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends._legacy:_Backend"
